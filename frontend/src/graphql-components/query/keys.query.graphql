query sshKeys($tenantId: String!) {
  tenant: itPolitoCrownlabsV1alpha2Tenant(name: $tenantId) {
    metadata {
      name
    }
    spec {
      email
      firstName
      lastName
      publicKeys
    }
  }
}
