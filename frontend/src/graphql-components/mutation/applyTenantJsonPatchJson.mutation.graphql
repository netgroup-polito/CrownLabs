mutation applyTenantJsonPatchJson(
  $tenantId: String!
  $patchJson: String!
  $manager: String!
) {
  applyTenant: patchCrownlabsPolitoItV1alpha2TenantJsonPatchJson(
    name: $tenantId
    fieldManager: $manager
    applicationJsonPatchJsonInput: $patchJson
  ) {
    metadata {
      name
    }
    spec {
      firstName
      lastName
      email
      lastLogin
      workspaces {
        role
        name
      }
    }
  }
}
