{{- if and .Values.webssh.expositionEnabled .Values.global.gateway.gatewayApiMode }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "bastion.fullname" . }}-webssh
  labels:
    {{- include "webssh.labels" . | nindent 4 }}
    crownlabs.polito.it/public-route: "true"
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.webssh.httproute.gateway.name | default .Values.global.gateway.name }}
      sectionName: https
{{- if .Values.webssh.httproute.gateway.namespace }}
      namespace: {{ .Values.webssh.httproute.gateway.namespace }}
{{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.webssh.httproute.path }}
      timeouts:
        request: {{ .Values.webssh.httproute.timeouts.request | default "600s" }}
        backendRequest: {{ .Values.webssh.httproute.timeouts.backendRequest | default "600s" }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "bastion.fullname" . }}-webssh
          port: {{ .Values.webssh.service.port }}
          weight: 1
{{- end }}

---

{{- if and .Values.webvnc.expositionEnabled .Values.global.gateway.gatewayApiMode }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "bastion.fullname" . }}-webvnc
  labels:
    {{- include "webvnc.labels" . | nindent 4 }}
    crownlabs.polito.it/public-route: "true"
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.webvnc.httproute.gateway.name | default .Values.global.gateway.name }}
      sectionName: https
{{- if .Values.webvnc.httproute.gateway.namespace }}
      namespace: {{ .Values.webvnc.httproute.gateway.namespace }}
{{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.webvnc.httproute.path }}
      timeouts:
        request: {{ .Values.webvnc.httproute.timeouts.request | default "600s" }}
        backendRequest: {{ .Values.webvnc.httproute.timeouts.backendRequest | default "600s" }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "bastion.fullname" . }}-webvnc
          port: {{ .Values.webvnc.service.port }}
          weight: 1
{{- end }}
