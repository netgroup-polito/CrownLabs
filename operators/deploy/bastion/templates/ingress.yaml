{{- if and .Values.webssh.expositionEnabled (not .Values.global.gateway.gatewayApiMode) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "bastion.fullname" . }}-webssh
  labels:
    {{- include "webssh.labels" . | nindent 4 }}
  {{- with .Values.webssh.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.webssh.ingress.hostname }}
      http:
        paths:
          - path: {{ .Values.webssh.ingress.path }}
            pathType: {{ .Values.webssh.ingress.pathType }}
            backend:
              service:
                name: {{ include "bastion.fullname" . }}-webssh
                port:
                  name: webssh
  {{- if .Values.webssh.ingress.secret }}
  tls:
    - hosts:
        - {{ .Values.webssh.ingress.hostname }}
      secretName: {{ .Values.webssh.ingress.secret }}
  {{- end }}
{{- end }}

---

{{- if and .Values.webvnc.expositionEnabled (not .Values.global.gateway.gatewayApiMode) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "bastion.fullname" . }}-webvnc
  labels:
    {{- include "webvnc.labels" . | nindent 4 }}
  {{- with .Values.webvnc.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.webvnc.ingress.hostname }}
      http:
        paths:
          - path: {{ .Values.webvnc.ingress.path }}
            pathType: {{ .Values.webvnc.ingress.pathType }}
            backend:
              service:
                name: {{ include "bastion.fullname" . }}-webvnc
                port:
                  name: webvnc
  {{- if .Values.webvnc.ingress.secret }}
  tls:
    - hosts:
        - {{ .Values.webvnc.ingress.hostname }}
      secretName: {{ .Values.webvnc.ingress.secret }}
  {{- end }}
{{- end }}
