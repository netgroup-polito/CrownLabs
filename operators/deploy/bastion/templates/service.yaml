apiVersion: v1
kind: Service
metadata:
  name: {{ include "bastion.fullname" . }}
  labels:
    {{- with .Values.serviceLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- include "bastion.labels" . | nindent 4 }}
{{- with .Values.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: ssh
      protocol: TCP
      name: ssh
  externalTrafficPolicy: {{ .Values.service.externalTrafficPolicy }}
  selector:
    {{- include "bastion.selectorLabels" . | nindent 4 }}

---

{{- if .Values.webssh.service }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bastion.fullname" . }}-webssh
  labels:
    {{- include "webssh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.webssh.service.type }}
  ports:
    - port: {{ .Values.webssh.service.port }}
      targetPort: {{ .Values.webssh.service.targetPort }}
      protocol: TCP
      name: webssh
      appProtocol: kubernetes.io/ws
  selector:
    {{- include "webssh.selectorLabels" . | nindent 4 }}
{{- end }}

---

{{- if .Values.webvnc.service }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bastion.fullname" . }}-webvnc
  labels:
    {{- include "webvnc.labels" . | nindent 4 }}
    {{- include "bastion.metricsAdditionalLabels" . | nindent 4 }}
spec:
  type: {{ .Values.webvnc.service.type }}
  ports:
    - port: {{ .Values.webvnc.service.port }}
      targetPort: {{ .Values.webvnc.service.targetPort }}
      protocol: TCP
      name: webvnc
  selector:
    {{- include "webvnc.selectorLabels" . | nindent 4 }}
{{- end }}
