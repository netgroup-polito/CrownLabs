apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-configuration-image-list-update
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "operator.labels" . | nindent 4 }}
data:
  registries: |
    {{- .Values.configurations.imageList.registries | toYaml | nindent 4 }}
