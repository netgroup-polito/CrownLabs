
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: templates.crownlabs.polito.it
spec:
  group: crownlabs.polito.it
  names:
    kind: Template
    listKind: TemplateList
    plural: templates
    shortNames:
    - tmpl
    singular: template
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.prettyName
      name: Pretty Name
      type: string
    - jsonPath: .spec.environmentList[0].image
      name: Image
      type: string
    - jsonPath: .spec.environmentList[0].environmentType
      name: Type
      type: string
    - jsonPath: .spec.environmentList[0].guiEnabled
      name: GUI
      type: string
    - jsonPath: .spec.environmentList[0].persistent
      name: Persistent
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Template describes the template of a CrownLabs environment to
          be instantiated.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TemplateSpec is the specification of the desired state of
              the Template.
            properties:
              deleteAfter:
                default: 7d
                description: The maximum lifetime of an Instance referencing the current
                  Template. Once this period is expired, the Instance may be automatically
                  deleted or stopped to save resources.
                pattern: ^[0-9]+[mhd]$
                type: string
              description:
                description: A textual description of the Template.
                type: string
              environmentList:
                description: The list of environments (i.e. VMs or containers) that
                  compose the Template.
                items:
                  description: Environment defines the characteristics of an environment
                    composing the Template.
                  properties:
                    environmentType:
                      description: The type of environment to be instantiated, among
                        VirtualMachine and Container.
                      enum:
                      - VirtualMachine
                      - Container
                      type: string
                    guiEnabled:
                      default: true
                      description: Whether the environment is characterized by a graphical
                        desktop or not.
                      type: boolean
                    image:
                      description: The VM or container to be started when instantiating
                        the environment.
                      type: string
                    name:
                      description: The name identifying the specific environment.
                      type: string
                    persistent:
                      default: false
                      description: Whether the environment should be persistent (i.e.
                        preserved when the corresponding instance is terminated) or
                        not.
                      type: boolean
                    resources:
                      description: The amount of computational resources associated
                        with the environment.
                      properties:
                        cpu:
                          description: The maximum number of CPU cores made available
                            to the environment (ranging between 1 and 8 cores). This
                            maps to the 'limits' specified for the actual pod representing
                            the environment.
                          format: int32
                          maximum: 8
                          minimum: 1
                          type: integer
                        memory:
                          anyOf:
                          - type: integer
                          - type: string
                          description: The amount of RAM memory assigned to the given
                            environment. Requests and limits do correspond to avoid
                            OOMKill issues.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        reservedCPUPercentage:
                          description: The percentage of reserved CPU cores, ranging
                            between 1 and 100, with respect to the 'CPU' value. Essentially,
                            this corresponds to the 'requests' specified for the actual
                            pod representing the environment.
                          format: int32
                          maximum: 100
                          minimum: 1
                          type: integer
                      required:
                      - cpu
                      - memory
                      - reservedCPUPercentage
                      type: object
                  required:
                  - environmentType
                  - image
                  - name
                  - resources
                  type: object
                type: array
              prettyName:
                description: The human-readable name of the Template.
                type: string
              workspace.crownlabs.polito.it/WorkspaceRef:
                description: The reference to the Workspace this Template belongs
                  to.
                properties:
                  name:
                    description: The name of the resource to be referenced.
                    type: string
                  namespace:
                    description: The namespace containing the resource to be referenced.
                      It should be left empty in case of cluster-wide resources.
                    type: string
                required:
                - name
                type: object
            required:
            - description
            - environmentList
            - prettyName
            type: object
          status:
            description: TemplateStatus reflects the most recently observed status
              of the Template.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
