apiVersion: template.crown.team.com/v1
kind: LabTemplate
metadata:
  name: labtemplate-sample
spec:
  # Add fields here
  vmSize: 100Mi
  imageToMount:
    - names:
        - "nginx"
    - size:
        200Ki
